import type { SystemDesignPath } from '../types'

export const capTheoremPath: SystemDesignPath = {
  id: 'cap-theorem',
  title: 'CAP Theorem',
  summary:
    'How a multi-server database behaves when the network between servers breaks, and why you must choose between one shared truth and always answering.',
  order: 3,
}
