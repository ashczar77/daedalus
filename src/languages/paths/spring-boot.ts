import type { LanguagePath } from '../types'

export const springBootPath: LanguagePath = {
  id: 'spring-boot',
  title: 'Spring Boot',
  summary: 'Auto-config, web, data, testing, actuator, and Boot interview traps.',
  order: 4,
}
