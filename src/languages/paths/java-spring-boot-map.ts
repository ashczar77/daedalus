import type { LanguagePath } from '../types'

export const javaSpringBootMapPath: LanguagePath = {
  id: 'java-spring-boot-map',
  title: 'Java · Spring · Boot map',
  summary: 'What each layer owns, and when you need which one.',
  order: 1,
}
