import type { LanguagePath } from '../types'

export const springCorePath: LanguagePath = {
  id: 'spring-core',
  title: 'Spring core',
  summary: 'IoC, beans, AOP, and framework interview traps - without Boot.',
  order: 3,
}
